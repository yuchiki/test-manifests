apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: home-chat-http-route
  namespace: home-chat
spec:
  parentRefs:
    - name: gateway
      namespace: istio-gateway
      sectionName: home
  hostnames: ["chat.home" ]
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: home-chat-service
          port: 3000
